/**
 * Using POST params update a part to the database
 * Redirects to /editrig after success
 */

 const requireOption = require('../common/requireOption');

 module.exports = function (objectrepository) {
     return function (req, res, next) {
         next();
     };
 };